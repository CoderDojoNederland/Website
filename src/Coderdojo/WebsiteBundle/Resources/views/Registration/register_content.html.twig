{% trans_default_domain 'FOSUserBundle' %}


<div class="panel panel-green margin-bottom-40">
    <div class="panel-heading">
        <h3 class="panel-title">Dojo Registreren</h3>
    </div>
    <div class="panel-body">
        <p>
            Dit formulier is bedoeld voor de Champions die een nieuwe dojo hebben opgezet. Is jouw dojo een feit? Dan kun je deze hier registreren zodat iedereen jouw dojo kan vinden.
        </p>
        <form action="{{ path('fos_user_registration_register') }}" {{ form_enctype(form) }} method="POST" class="fos_user_registration_register form-horizontal" role="form">
            {{ form_widget(form) }}
            <div class="form-group">
                <div class="col-lg-offset-2 col-lg-10">
                    <p style="color:#777777;font-style: italic;">Organiser is de <a href="http://apps.appypie.com/pages/eventbrite-tutorial#part1" target="_blank">organisator ID van Eventbrite</a>. Hiermee halen we automatisch je geplande dojo's op. Let op! De dojo wordt alleen weergegeven als deze live en publiek is en tevens nog beschikbare registraties heeft.</p>
                </div>
            </div>
            <div class="form-group">
                <div class="col-lg-offset-2 col-lg-10">
                    <input type="submit" value="{{ 'registration.submit'|trans }}" class="btn-u btn-u-green" />
                </div>
            </div>
        </form>
    </div>
</div>