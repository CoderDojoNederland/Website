{% trans_default_domain 'FOSUserBundle' %}

<div class="panel panel-green margin-bottom-40">
    <div class="panel-heading">
        <h3 class="panel-title">Dojo Gegevens</h3>
    </div>
    <div class="panel-body">
        <form action="{{ path('fos_user_profile_edit') }}" {{ form_enctype(form) }} method="POST" class="fos_user_profile_edit form-horizontal" role="form">
            {{ form_widget(form) }}
            <div class="form-group">
                <div class="col-lg-offset-2 col-lg-10">
                    <input type="submit" value="{{ 'profile.edit.submit'|trans }}" class="btn-u btn-u-green" />
                </div>
            </div>
            <div>

            </div>
        </form>

    </div>
</div>