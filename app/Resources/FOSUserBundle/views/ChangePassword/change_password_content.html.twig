{% trans_default_domain 'FOSUserBundle' %}

<div class="panel panel-green margin-bottom-40">
    <div class="panel-heading">
        <h3 class="panel-title margin-bottom-10">Wachtwoord aanpassen</h3>
    </div>
    <div class="panel-body">
        {{ form_start(form, { 'action': path('fos_user_change_password'), 'attr': { 'class': 'fos_user_change_password form-horizontal' } }) }}
        {{ form_widget(form) }}
        <div class="form-group">
            <div class="col-lg-offset-2 col-lg-10">
                <input type="submit" value="{{ 'profile.edit.submit'|trans }}" class="btn-u btn-u-green" />
            </div>
        </div>
        <div>

        </div>
        {{ form_end(form) }}

    </div>
</div>
